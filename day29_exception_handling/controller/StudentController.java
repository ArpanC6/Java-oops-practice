package day29_exception_handling.controller;

public class StudentController {

    /*
     GET /students/{id}

     if student not found:
     throw new StudentNotFoundException("Student not found with id " + id);
    */
}
